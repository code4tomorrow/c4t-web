export enum Pages {
    INDEX = "/",
    COURSES = "/courses",
    LEARN = "/learn",
    VOLUNTEER = "/volunteer",
    ABOUT = "/about",
    API_JOB_PREVIEW = "/api/contentful/jobs/preview",
    API_JOB = "/api/contentful/jobs"
}